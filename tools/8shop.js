/**
 * @module $shop
 * author : eray arslan
 *
 * 8art javascript array designer
 *
 * @param {Object} o
 * @return {$shop}
 */

var $shop = function $shop(o) {
    if (!(this instanceof $shop)) {
        return new $shop(o);
    }
    //
    return this;
};